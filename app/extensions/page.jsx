import React from "react";
import Extensions from "@/extensions/components/extensions";

export const metadata = {
  title: "Extensions",
  description: "Get Extensions info. Start and stop the extensions.",
};

export default function Page() {
  return <Extensions />;
}
